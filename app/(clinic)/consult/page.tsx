/**
 * The consulting room. Built in M1 (PLAN.md §8): patients, appointments,
 * tokens, queue, consult form, Rx composer, prescription print.
 *
 * The route exists now so the layout, the auth gate and the tablet viewport are
 * exercised from the first commit rather than bolted on at M1.
 */
export default function ConsultPage() {
  return <h1 className="text-2xl font-semibold">Consult</h1>;
}
