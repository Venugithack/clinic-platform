/**
 * The pharmacy counter. Built across M2 (the live link), M3 (dispense) and
 * M4 (billing and the till).
 *
 * M2 is the demo — a prescription signed on the doctor's tablet appearing here
 * in under a second is the feature the clinic actually bought, and it is shown
 * the day it works (BUILD.md §4).
 */
export default function CounterPage() {
  return <h1 className="text-2xl font-semibold">Counter</h1>;
}
